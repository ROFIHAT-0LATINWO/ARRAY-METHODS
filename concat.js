let items=["rice", "maize", "soya", "oat", "millet"];
let words=["they are all grains"];
let both= items.concat(words);
    console.log(both)//it merge two or more arrays together to become a single array.